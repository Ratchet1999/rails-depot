<%= form_with(model: add_quantity_to_line_item) do |form| %>
  <% if add_quantity_to_line_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(add_quantity_to_line_item.errors.count, "error") %> prohibited this add_quantity_to_line_item from being saved:</h2>

      <ul>
        <% add_quantity_to_line_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
